import { RoleButtonDirective } from './role-button.directive';

describe('RoleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
